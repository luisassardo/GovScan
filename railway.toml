[build]
builder = "railpack"

[build.config]
pythonVersion = "3.11"
installCmd = "pip install -r requirements.txt"

[deploy]
startCommand = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/api/methodology"
healthcheckTimeout = 30
